package com.journiapp.stampapplication.model

/** user information as stored in user preferences.
 *
 * Currently only contains the session cookie which authenticates the user. */
class UserInformation {

    var sessionCookie: String = ""
}